{
  "name": "linear-sdk-axi",
  "version": "0.1.0",
  "description": "AXI-compliant Linear CLI wrapping @linear/sdk — token-efficient TOON output for agents",
  "type": "module",
  "keywords": [
    "linear",
    "cli",
    "agent",
    "axi",
    "toon"
  ],
  "bin": {
    "linear-sdk-axi": "./dist/bin/linear-sdk-axi.js"
  },
  "files": [
    "dist",
    "skills/linear-sdk-axi",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc && chmod +x dist/bin/linear-sdk-axi.js",
    "test": "npm run build && vitest run",
    "dev": "tsx bin/linear-sdk-axi.ts"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@linear/sdk": "^62.0.0",
    "@toon-format/toon": "^2.1.0",
    "axi-sdk-js": "^0.1.10"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
